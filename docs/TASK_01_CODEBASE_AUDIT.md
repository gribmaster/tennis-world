# TASK 01 — Codebase health audit (READ-ONLY)

Task:
Perform a read-only health audit of this monorepo and produce a single written report.
No feature work. Establish the true current state of the code, because the last
checkpoint doc is from 2026-07-15 and the last commits are from 2026-07-28.

Context:
- Repo root: D:\work\tennis — Tennis World / tennismap.app monorepo.
- `CLAUDE.md` at the repo root is the standing rulebook; read it first and treat it as
  authoritative. `docs/PHASE_5_COMPLETION_SUMMARY.md`, `docs/PHASE_4_COMPLETION_SUMMARY.md`,
  `docs/PHASE_2_COMPLETION_SUMMARY.md` and `tennis-world-full-checkpoint-2026-07-15.md`
  record the as-built state of phases 2/4/5.
- IGNORE `docs/Claude Ready Prompt — Tennis Project.md` and
  `docs/Tennis Project Full Report.md` (and their copies at the repo root). They were
  generated by another tool and describe a DIFFERENT application (a couples "cards" app
  with Connection/Intimacy/Lovemaking categories, Next.js API routes, Supabase-direct
  Prisma). They are factually wrong about this repo. Do not use them as input; do note
  in the report that they should be deleted.
- Phase 3 (admin panel) has never been started; `apps/admin` is a deliberate placeholder.

Requirements:

1. Toolchain and install
   - Report node version, pnpm version, and whether they satisfy the `engines` /
     `packageManager` pins in the root `package.json`.
   - Run `pnpm install --frozen-lockfile`. Report whether the lockfile is in sync.

2. Static checks — run and report exact pass/fail with error counts
   - `pnpm --filter @tennis/api prisma:generate`
   - `pnpm lint`
   - `pnpm typecheck`
   - `pnpm build`
   For every failure: file, error code, one-line cause. Do NOT fix anything.

3. Verification harnesses
   - Enumerate every `verify:*` script that exists at the root and in each workspace
     (read the package.json files, do not guess).
   - For each, read its header comment and classify: runnable offline / needs a running
     API / needs a seeded DB / needs Stripe secrets.
   - Run the ones that need nothing beyond a local build, and report their pass/fail
     counts. For the rest, state exactly what is missing. Do NOT start Docker, do NOT
     create or migrate any database, do NOT run anything that touches staging or
     production.

4. Working-tree forensics
   - `git status` shows 12 modified `content/*/info.txt` files and a modified
     `apps/web/tsconfig.json`. For each: show the diff and explain in one line what
     changed and whether it looks intentional or accidental.
   - List everything untracked (`docs/`, `backups/`, `.claude/`, the two stray root
     `.md` files) and recommend for each: commit / gitignore / delete. Check whether
     `backups/` or anything untracked contains secrets or database dumps.
   - Do NOT stage, commit, delete, or revert anything. Recommendations only.

5. Drift between CLAUDE.md and the code
   - Verify each factual claim in CLAUDE.md §1 (structure), §2 (commands), §3 (ports,
     env var names, DATABASE_URL vs DIRECT_URL) against the actual files.
   - Report every claim that no longer matches reality, with the correct value.

6. Post-checkpoint commits
   - The three most recent commits (442d56c "map index fix", a0a0618 "map fix",
     041b35c "update user profile photo") landed after the last checkpoint doc.
   - Summarize what each actually changed (files + behavior) and whether it touched
     anything covered by a `verify:*` harness or by the CLAUDE.md §4 pending-state rules.

7. Dependency and version reality check
   - Report the real installed major versions of: next, react, @nestjs/core, prisma /
     @prisma/client, stripe, tailwindcss.
   - Flag any package that is more than one major version behind, but do NOT upgrade.

8. Open-item reconciliation
   - `tennis-world-full-checkpoint-2026-07-15.md` §30 lists "Needs verification" and
     "Not yet final" items. For each item, state from the code whether it is now Done /
     Still open / Cannot tell without a running environment. Be specific about which
     file or route you checked.

Do not change:
- Any source file, config, schema, migration, or lockfile.
- No `pnpm add` / `pnpm update` / package installs of any kind.
- No git operations that mutate state (no add/commit/checkout/stash/clean/restore).
- No database creation, migration, seeding, or reset. Nothing against staging or production.
- No Stripe calls.
This task is strictly read + run-checks + report.

Testing:
The checks listed in requirements 2 and 3 ARE the testing. Report raw pass/fail counts,
not summaries of them.

Report back:
Write the full report to `docs/AUDIT_2026_09_07.md` with these sections:
  1. Verdict — is the repo currently green, and what is the single biggest risk
  2. Toolchain & install
  3. Static checks (lint / typecheck / build) — results table
  4. Verify harnesses — table: script | requires | result
  5. Working-tree forensics — table: path | change | recommendation
  6. CLAUDE.md drift — table: claim | reality
  7. Post-checkpoint commits
  8. Dependency versions
  9. Open-item reconciliation
  10. Recommended next 3 tasks, ranked, with a one-line justification each

Then, in the chat, give me only: the verdict paragraph, the failure counts, and the
ranked next-3 list.
